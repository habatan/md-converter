FROM python:3.11-slim
WORKDIR /app

# システムの依存関係（Word変換用のPandocなど）をインストール
RUN apt-get update && apt-get install -y pandoc && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN ["pip", "install", "--no-cache-dir", "-r", "requirements.txt"]
